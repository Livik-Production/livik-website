import OpenResumePage from "@/modules/OpenResumePage";

export const metadata = {
  title: "Open Application | Livik",
  description: "Join Livik's growing team. Send us your resume and build the future with us.",
};

export default function Page() {
  return <OpenResumePage />;
}
